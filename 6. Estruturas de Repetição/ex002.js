var numero = 3;
var contador = 0;

console.log ("Tabuada de : " + numero);

while (contador <= 10) {
    var resultado = numero * contador;
    console.log (numero + "x" + contador + " = " + resultado);
    contador++;
}