// Introdução:

// if / else; else if; switch.

// Estrutura condicional (if)
if (idade >= 18) {
    console.log ('Maior de idade, pode ser preso');
}
else {
    console.log ('Menor de idade, vai tomar seu toddynho');
}

// Estrutura condicional (switch)
switch (ddd) {
    case 11:
        console.log ("São Paulo");
        break;
    case 61:
        console.log ("Brasília")
        break;
}

// Estruturas condicionais são úteis quando precisamos que o sistema toma uma ou mais decisões sem a intervenção do programador.