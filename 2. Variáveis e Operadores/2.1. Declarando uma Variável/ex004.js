// Tipos de dados : array

var cidades = ["Sobradinho", "Brasilinha", "Planaltina"];

// para imprimir espeficicamente precisamos selecionar qual array usar
// para isso usamos a variável e o numero do array
// lembrando que o primeiro sempre começa com 0

console.log (cidades[1]);
console.log (cidades[2]);
console.log (cidades[0]);

// alterar o valor de uma array

var nomes = ["Walison", "Mario", "Yuri"];
var nome = "Smash";
nomes[2] = nome;

console.log = (nomes[2]);

// Quando usar um array?
// Armazenamento de informações

var menu = ["Home", "Sobre", "Contato"]